<!doctype html>
<html lang="en">
    <head></head>
    <body>
        <h1>Geschichtsstationen Moers</h1>
            <table border=1 cellpadding=7>
            <tr>
                <th>Name</th>
            </tr>
            {% for object in object_list['features'] %}
            <tr>
            <td><a href="{{ object['properties']['ID'] }}/">{{ object['properties']['Name'] }}</a></td>
            
            </tr>
            {% endfor %}
        </table>
    </body>
</html>
